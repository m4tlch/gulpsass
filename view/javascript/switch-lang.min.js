!function () {
  var a = {
    _init: function () {
      var e = a;
      $(".lang-swicher_element").on("click", function () {
        var a = $(this).data("btn-lang");
        $('.lang-swicher-menu').slideUp(200);
        e.langToggle(a)
      }), e.langToggle(navigator.language || navigator.browserLanguage)
    },
    langToggle: function (e) {
      var n = /\w\w/,
        t = n.exec(e) + "",
        g = $("[data-lang=" + t + "]").find("img"),
        i = a;
      switch (t) {
        case "ru":
        case "ua":
        case "by":
          e = "ru";
          break;
        case "de":
          e = "de";
          break;
        default:
          e = "en"
      }
      $.cookie('user_lang', e, {expires: 2});
      $(".lang-swicher_element").removeClass("active"),
        $(".lang-swicher_element-" + e).addClass("active"),
        $("[data-lang]").filter('[data-lang!="' + e + '"]').hide().siblings('[data-lang="' + e + '"]').show(), g.each(i.getSrc)
    },
    getSrc: function () {
      return $(this).attr("src", $(this).data("src"))
    }
  };
  $(document).ready(a._init)
}();